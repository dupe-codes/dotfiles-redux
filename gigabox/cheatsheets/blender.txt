middle mouse := rotate view
shift + middle mouse := translate view
g := grab and move object, escape/right click to cancel
r := rotate object, escape/right click to cancel
s := scale object, escape/right click to cancel
z/x/y := constrain movement to the chosen axis. double tap for local axis.
shift + right click := move 3d cursor around
shift + a := add object
ctrl + select := remove selections
shift + d := duplicate object
shift + z/x/y := EXCLUDE an axis
tab := enter edit mode
numpad . := frame selection
numpad 1 := y axis viewpoint
numpad 3 := x axis viewpoint
numpad 7 := z axis viewpoint
1 := vertex select (edit mode)
2 := edge select (edit mode)
3 := face select (edit mode)
p := in edit mode, separate parts
ctrl + r := loop cut tool
alt + a := deselect all
alt + left click := select entire edge loop, all edges stopping at "pole" vertex
e := extrude tool
x := delete selection
ctrl + l := link/transfer data
. := open transform pivot point menu
n := open item menu
0 := look through camera
gg := slide edge
gg + e := slide edge conformed to top shape
gg + f := slide edge conformed to bottom shape
i := add inset
ctrl + a := apply transformations (reset to "global")
wheel or +/- (loop cut) := add more cuts