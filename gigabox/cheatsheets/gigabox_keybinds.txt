<leader> shift space := utility script menu
<leader> esc := open power menu
<leader> space := open app launcher
<leader> p := open screenshotting menu
<leader> c := open calculator
<leader> shift tab := view clipboard history
<leader> shift [w a s d ...] := reposition window
<leader> shift m := swap window between monitors
<leader> shift [c f] := center/fullscreen window
<leader> ctrl q/e := cycle windows on current desktop
<leader> ctrl [w a s d] := directionally select current desktop windows
<leader> tab := cycle windows on all desktops
<leader> [1 2 ... 5] := go to desktop
<leader> shift [1 2 ... 5] := send to desktop
<leader> q := close current window
<leader> t := toggle window decorations
<leader> x := toggle fullscreen
<leader> d := toggle show desktop
<leader> n := add quick note
<leader> shift n := add to favorited note
<leader> m := add quick thought
<leader> s := open cheatsheets menu
<leader> l := add todo
<leader> w := open bookmarks
<leader> shift l := view the quest log
<leader> shift h := view character stats
<leader> shift t := view schedule (timetable)
ctrl esc := view last notification
ctrl return := open notification context
ctrl shift esc := close last notification
ctrl shift return := close all notifications
